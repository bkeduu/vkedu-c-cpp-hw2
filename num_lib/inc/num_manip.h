#pragma once

#include <stdlib.h>

#include "num_utils.h"

int get_max(size_t* arr, size_t size);
long get_sum(size_t* arr, size_t size);

size_t get_dig_cnt(int);

int comp(const int*, const int*);

void m_sort(array_t*);
