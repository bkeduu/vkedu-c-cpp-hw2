#pragma once

#include <stdlib.h>

#include "num_utils.h"

int get_max(const size_t*, size_t);
long get_sum(const size_t*, size_t);
size_t get_dig_cnt(int);
int comp(const int*, const int*);

void sort(array_t);
short mp_sort(array_t**, size_t, size_t);

